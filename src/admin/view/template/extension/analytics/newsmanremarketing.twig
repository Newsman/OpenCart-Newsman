{{ header }}{{ column_left }}
<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <div class="pull-right">
                <button type="submit" form="form-analytics" data-toggle="tooltip" title="{{ button_save }}"
                        class="btn btn-primary"><i class="fa fa-save"></i></button>
                <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i
                            class="fa fa-reply"></i></a></div>
            <h1><img src="{{ logo }}" alt="{{ heading_title }}" style="height: 30px;" /></h1>
            <span class="help-block"><a href="https://www.newsman.com/" target="_blank">www.newsman.com</a> | {{ text_version }}: {{ newsman_version }}</span>
            <ul class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ul>
            |&nbsp;<a href="{{ url_newsman_settings }}" title="{{ text_newsman_settings }}">{{ text_newsman_settings }}</a>
        </div>
    </div>
    <div class="container-fluid">
        <div class="well">
            <div class="row">
                <div class="col-sm-12">
                    <div class="form-group">
                        <label class="control-label" for="input-store">{{ text_store }}</label>
                        <select name="store_id" id="input-store" class="form-control" onchange="location = this.value;">
                            {% for store in stores %}
                                <option value="{{ store.href }}" {% if store.store_id == store_id %}selected{% endif %}>{{ store.name }}</option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
            </div>
        </div>
        {% if error_warning %}
            <div class="alert alert-danger alert-dismissible"><i
                        class="fa fa-exclamation-circle"></i> {{ error_warning }}
                <button type="button" class="close" data-dismiss="alert">&times;</button>
            </div>
        {% endif %}
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title"><i class="fa fa-pencil"></i> {{ text_edit }}</h3>
            </div>
            <div class="panel-body">
                <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form-analytics"
                      class="form-horizontal">
                    <div class="alert alert-info"><i class="fa fa-info-circle"></i> {{ text_signup }}
                        <button type="button" class="close" data-dismiss="alert">&times;</button>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-api-status">
                            <span data-toggle="tooltip" title="" data-original-title="{{ text_api_status_hint }}">{{ entry_api_status }}</span>
                        </label>
                        <div class="col-sm-10">
                            <label class="control-label" for="input-api-status">
                                {% if is_remarketing_connected %}
                                    <strong style="color: #40826D;">{{ text_credentials_valid }}</strong>
                                {% else %}
                                    <strong style="color: #D2042D;">{{ text_credentials_invalid }}</strong>
                                {% endif %}
                            </label>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-code">{{ entry_tracking }}</label>
                        <div class="col-sm-10">
                            <input name="analytics_newsmanremarketing_trackingid" placeholder="{{ entry_tracking }}"
                                   id="input-code" class="form-control"
                                   value="{{ analytics_newsmanremarketing_trackingid }}">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-status">{{ entry_status }}</label>
                        <div class="col-sm-10">
                            <select name="analytics_newsmanremarketing_status" id="input-status" class="form-control">
                                <option value="1"
                                        {% if analytics_newsmanremarketing_status %}selected{% endif %}>{{ text_enabled }}</option>
                                <option value="0"
                                        {% if not analytics_newsmanremarketing_status %}selected{% endif %}>{{ text_disabled }}</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-anonymize-ip">{{ entry_anonymize_ip }}</label>
                        <div class="col-sm-10">
                            <select name="analytics_newsmanremarketing_anonymize_ip" id="input-anonymize-ip" class="form-control">
                                <option value="1"
                                        {% if analytics_newsmanremarketing_anonymize_ip %}selected{% endif %}>{{ text_enabled }}</option>
                                <option value="0"
                                        {% if not analytics_newsmanremarketing_anonymize_ip %}selected{% endif %}>{{ text_disabled }}</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-send-telephone">{{ entry_send_telephone }}</label>
                        <div class="col-sm-10">
                            <select name="analytics_newsmanremarketing_send_telephone" id="input-send-telephone" class="form-control">
                                <option value="1"
                                        {% if analytics_newsmanremarketing_send_telephone %}selected{% endif %}>{{ text_enabled }}</option>
                                <option value="0"
                                        {% if not analytics_newsmanremarketing_send_telephone %}selected{% endif %}>{{ text_disabled }}</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label" for="input-theme-cart-compatibility">{{ entry_theme_cart_compatibility }}</label>
                        <div class="col-sm-10">
                            <select name="analytics_newsmanremarketing_theme_cart_compatibility" id="input-theme-cart-compatibility" class="form-control">
                                <option value="1"
                                        {% if analytics_newsmanremarketing_theme_cart_compatibility %}selected{% endif %}>{{ text_enabled }}</option>
                                <option value="0"
                                        {% if not analytics_newsmanremarketing_theme_cart_compatibility %}selected{% endif %}>{{ text_disabled }}</option>
                            </select>
                            <span class="help-block">{{ entry_theme_cart_compatibility_help }}</span>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{{ footer }} 
